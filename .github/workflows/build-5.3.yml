name: Build Maktab Islami 5.3

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    name: Build APK 5.3
    runs-on: ubuntu-latest

    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Install required tools
        run: |
          set -e

          sudo apt-get update
          sudo apt-get install -y unzip findutils

      - name: Select FINAL 5.3 ZIP
        id: find_zip
        shell: bash
        run: |
          set -e

          ZIP_FILE="./Maktab-Islami-5.3-FULL-FINAL.zip"

          echo "Checking for FINAL 5.3 package..."
          echo "Expected ZIP: $ZIP_FILE"

          if [ ! -f "$ZIP_FILE" ]; then
            echo ""
            echo "ERROR: FINAL 5.3 ZIP was not found."
            echo ""
            echo "ZIP files currently in repository:"
            find . \
              -type f \
              -iname "*.zip" \
              ! -path "./.git/*" \
              -print | sort

            exit 1
          fi

          echo ""
          echo "FINAL 5.3 ZIP FOUND:"
          ls -lh "$ZIP_FILE"

          echo "zip_file=$ZIP_FILE" >> "$GITHUB_OUTPUT"

      - name: Verify FINAL ZIP
        working-directory: .
        shell: bash
        run: |
          set -e

          ZIP_FILE="${{ steps.find_zip.outputs.zip_file }}"

          echo "Verifying ZIP archive..."
          unzip -t "$ZIP_FILE"

          echo ""
          echo "ZIP verification completed successfully."

      - name: Extract FINAL project
        run: |
          set -e

          rm -rf extracted
          mkdir -p extracted

          ZIP_FILE="${{ steps.find_zip.outputs.zip_file }}"

          echo "Extracting:"
          echo "$ZIP_FILE"

          unzip -q "$ZIP_FILE" -d extracted

          echo ""
          echo "Extraction completed."

          echo ""
          echo "Extracted directory structure:"
          find extracted -maxdepth 7 -type d -print | sort

          echo ""
          echo "Important files:"
          find extracted \
            -type f \
            \
              -name "gradlew" \
              -o -name "settings.gradle" \
              -o -name "settings.gradle.kts" \
              -o -name "build.gradle" \
              -o -name "build.gradle.kts" \
              -o -name "AndroidManifest.xml" \
            \
            -print | sort

      - name: Find Android project
        id: find_project
        shell: bash
        run: |
          set -e

          echo "Searching for Android Gradle project..."

          GRADLEW=$(find extracted \
            -type f \
            -name "gradlew" \
            ! -path "*/.git/*" \
            -print | head -n 1)

          if [ -n "$GRADLEW" ]; then
            PROJECT_DIR=$(dirname "$GRADLEW")

            echo ""
            echo "Gradle wrapper found:"
            echo "$GRADLEW"

            echo ""
            echo "Android project directory:"
            echo "$PROJECT_DIR"

            echo "project_dir=$PROJECT_DIR" >> "$GITHUB_OUTPUT"
            echo "use_wrapper=true" >> "$GITHUB_OUTPUT"

            exit 0
          fi

          echo "No gradlew found."
          echo "Searching for Gradle settings file..."

          SETTINGS_FILE=$(find extracted \
            -type f \
            \
              -name "settings.gradle" \
              -o -name "settings.gradle.kts" \
            \
            ! -path "*/.git/*" \
            -print | head -n 1)

          if [ -n "$SETTINGS_FILE" ]; then
            PROJECT_DIR=$(dirname "$SETTINGS_FILE")

            echo ""
            echo "Gradle settings file found:"
            echo "$SETTINGS_FILE"

            echo ""
            echo "Android project directory:"
            echo "$PROJECT_DIR"

            echo "project_dir=$PROJECT_DIR" >> "$GITHUB_OUTPUT"
            echo "use_wrapper=false" >> "$GITHUB_OUTPUT"

            exit 0
          fi

          echo "No settings file found."
          echo "Searching for Gradle build file..."

          BUILD_FILE=$(find extracted \
            -type f \
            \
              -name "build.gradle" \
              -o -name "build.gradle.kts" \
            \
            ! -path "*/.git/*" \
            -print | head -n 1)

          if [ -n "$BUILD_FILE" ]; then
            PROJECT_DIR=$(dirname "$BUILD_FILE")

            echo ""
            echo "
